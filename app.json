{
  "name": "Looker Slackbot",
  "description": "A Slack bot for Looker, to make sharing data easier!",
  "repository": "https://github.com/looker/looker-slackbot",
  "keywords": ["looker", "bi", "analytics", "slack", "chat", "data"],
  "env": {
    "SLACK_API_KEY": {
      "description": "A Slack API token (find it on https://api.slack.com/web)",
      "required": true
    },
    "LOOKERS": {
      "description": "A JSON array of JSON objects, each representing a Looker instance and its authentication information. 'url' should be the web url of the instance, 'apiBaseUrl' should be the API 3.0 endpoint, 'clientID' should be the API 3.0 client ID for the user you want the bot to run as, 'clientSecret' should be the secret for that API 3.0 key, and 'customCommandSpaceId' is an optional parameter, representing a Space that you would like the bot to use to define custom commands.",
      "required": false,
      value: "[{\"url\": \"https://mycompany.looker.com\", \"apiBaseUrl\": \"https://mycompany.looker.com:19999/api/3.0\", \"clientId\": \"abc123\", \"clientSecret\": \"abc123\", \"customCommandSpaceId\": 1202}]"
    },
    "SLACKBOT_S3_BUCKET": {
      "description": "If you want to use the Slack bot to post visualization images, provide an Amazon S3 bucket name.",
      "required": false
    },
    "AWS_ACCESS_KEY_ID": {
      "description": "If you want to use the Slack bot to post visualization images, provide an Amazon S3 access key that can write to the provided bucket.",
      "required": false
    },
    "AWS_SECRET_ACCESS_KEY": {
      "description": "If you want to use the Slack bot to post visualization images, provide an Amazon S3 secret access key that can write to the provided bucket.",
      "required": false
    }
  }
}
